# ============================================================================
# REQUIRED: Core ML & Vision Framework
# ============================================================================
ultralytics==8.1.0              # YOLOv8 detection framework (worker + PPE detection)
torch==2.1.2                    # PyTorch deep learning library (GPU acceleration)
torchvision==0.16.2             # Vision utilities (model loading, image preprocessing)
opencv-python==4.8.1.78         # Video I/O and image processing (camera, video files)
numpy==1.24.3                   # Numerical arrays (detection processing, confidence scoring)

# ============================================================================
# OPTIONAL: ONNX & Cross-Platform Inference
# ============================================================================
onnx==1.15.0                    # ONNX model format (for model export, cross-platform inference)
onnxruntime==1.17.0             # ONNX Runtime (alternative to PyTorch, faster on some hardware)
# onnxruntime-qnn==1.17.0       # [OPTIONAL] Qualcomm QNN Runtime (uncomment for Rubik Pi, Snapdragon)

# ============================================================================
# REQUIRED: Configuration & Data Processing
# ============================================================================
pyyaml==6.0                     # YAML parsing (compliance rules, model config, inference settings)
pillow==10.0.0                  # Image processing (frame resizing, format conversion)
albumentations==1.3.1           # Data augmentation (training data preprocessing)

# ============================================================================
# OPTIONAL: Roboflow Cloud Inference Backend
# ============================================================================
# Uncomment to enable Roboflow cloud PPE detection (no local GPU needed)
inference-sdk>=0.9.0            # Roboflow cloud API client (InferenceHTTPClient)

# ============================================================================
# OPTIONAL: Dataset Acquisition (Training Only)
# ============================================================================
roboflow>=1.1.0                 # Download Roboflow Universe datasets
kaggle>=1.6.0                   # Download Kaggle datasets (SH17 Construction PPE)
huggingface-hub>=0.20.0         # Download pretrained models from HuggingFace Hub

# ============================================================================
# DEVELOPMENT: Testing & Code Quality
# ============================================================================
pytest==7.4.3                   # Unit test framework
pytest-cov==4.1.0               # Code coverage reporting

# Code quality tools
black==23.11.0                  # Code formatter (Python PEP 8)
flake8==6.1.0                   # Linter (code style checks)
isort==5.12.0                   # Import sorter (organize imports)

# ============================================================================
# UTILITIES
# ============================================================================
tqdm==4.66.1                    # Progress bars (training, dataset processing)
